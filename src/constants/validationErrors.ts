export const INVALID_ISBN_10 = (reason: string) => `Invalid ISBN-10 (${reason})`;
export const INVALID_ISBN_13 = (reason: string) => `Invalid ISBN-13 (${reason})`;
export const MAX_LENGTH = (max: number) => `Maximum of ${max} characters are allowed`;
export const REQUIRED = 'Required';
