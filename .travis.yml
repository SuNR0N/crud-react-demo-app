language: node_js

node_js:
  - "stable"

cache:
  directories:
   - node_modules

script:
  - yarn build-css
  - yarn test:coverage
  - yarn build

jobs:
  include:
    - script: cat coverage/lcov.info | node node_modules/.bin/coveralls