language: node_js

node_js:
  - "stable"

cache:
  directories:
   - node_modules

script:
  - yarn build-css
  - yarn test:coverage --maxWorkers=4 --coverageReporters=text-lcov | coveralls
  - yarn build