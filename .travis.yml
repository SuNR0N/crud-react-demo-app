language: node_js

node_js:
  - "stable"

cache:
  directories:
   - node_modules

script:
  - yarn build-css
  - yarn test:coverage
  - yarn build

after_script:
  - cat coverage/lcov.info | yarn coveralls